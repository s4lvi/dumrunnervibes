{
    "name": "dumrunner-vibes",
    "version": "1.0.0",
    "scripts": {
        "start": "npm --prefix client run start",
        "heroku-postbuild": "npm --prefix client run build",
        "postinstall": "npm install --prefix client"
    },
    "engines": {
        "node": "^20.x"
    },
    "dependencies": {
        "@tailwindcss/postcss": "^4.1.0",
        "next": "^15.2.4"
    }
}
